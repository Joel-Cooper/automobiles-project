import { FilterAutomobilesPipe } from './filter-automobiles.pipe';

describe('FilterAutomobilesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAutomobilesPipe();
    expect(pipe).toBeTruthy();
  });
});
