import { SortAutomobilesPipe } from './sort-automobiles.pipe';

describe('SortAutomobilesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAutomobilesPipe();
    expect(pipe).toBeTruthy();
  });
});
